# Synthetic Samples

Samples are self-made to avoid external document copyright issues.

Run:

```bash
uv run --project sidecar python samples/generate_samples.py
```

Generated assets are written to `samples/generated/` and cover:

- Formula screenshot.
- Table page.
- Mixed Chinese/English page.
- Multi-page PDF source image set where dependencies allow generation.

